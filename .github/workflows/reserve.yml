name: Reserve file
on:
  discussion_comment:
    types:
      - edited
    tags:
      - аанкылара
  workflow_dispatch:
    inputs:
      name:
        description: 'Reserve a file'
jobs:
  reserve_file:
    runs-on: ubuntu-latest
    steps:
      - name: reserve file
        with:
          repo-token: ${{ secrets.PUSH_TO_PROTECTED_BRANCH }}
        run: | 
             ls
